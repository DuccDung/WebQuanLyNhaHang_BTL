@model WebQuanLyNhaHang.ViewModel.ViewModelBan
@{
    Layout = null;
    var tables = Model.TableCards();
}
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Đơn tại quán</title>
    <link rel="icon" type="image/svg+xml" href="~/assets/images/admin-favicon.svg" asp-append-version="true" />
    <link rel="shortcut icon" href="~/assets/images/admin-favicon.svg" asp-append-version="true" />
    <link rel="stylesheet" href="~/assets/css/admin-dashboard.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/assets/css/admin-tables.css" asp-append-version="true" />
</head>
<body>
    <div class="dashboard-app">
        @await Html.PartialAsync("_AdminSidebar")

        <button class="mobile-nav-backdrop" id="mobile-nav-backdrop" type="button" aria-label="Đóng menu" hidden></button>

        <div class="main-shell">
            <header class="topbar topbar--tables">
                <button class="icon-button mobile-nav-trigger" id="mobile-nav-trigger" type="button" aria-label="Mở menu" aria-controls="dashboard-sidebar" aria-expanded="false">
                    <svg viewBox="0 0 24 24" fill="none">
                        <path d="M4 7h16M4 12h16M4 17h16" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path>
                    </svg>
                </button>

                <div class="topbar__actions">
                    <button class="icon-button notification-button" type="button" aria-label="Thông báo">
                        <svg viewBox="0 0 24 24" fill="none">
                            <path d="M15 17H5.6a1 1 0 0 1-.8-1.6L6 14V10a6 6 0 1 1 12 0v4l1.2 1.4a1 1 0 0 1-.8 1.6H15ZM10 20a2 2 0 0 0 4 0" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path>
                        </svg>
                        <span class="notification-dot"></span>
                    </button>

                    <div class="account-menu">
                        <button class="account-trigger" id="account-trigger" type="button" aria-expanded="false">
                            <span class="account-trigger__avatar">@Model.Initials</span>
                            <span class="account-trigger__copy">
                                <strong>@Model.AdminDisplayName</strong>
                                <small>@Model.AdminAccount</small>
                            </span>
                            <span class="account-trigger__chevron" aria-hidden="true">
                                <svg viewBox="0 0 24 24" fill="none">
                                    <path d="m6 9 6 6 6-6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path>
                                </svg>
                            </span>
                        </button>

                        <div class="account-dropdown" id="account-dropdown" hidden>
                            <div class="account-dropdown__header">
                                <strong>@Model.AdminDisplayName</strong>
                                <span>@Model.AdminAccount</span>
                            </div>
                            <a class="account-dropdown__item" href="@Url.Action("Index", "Admin")">Dashboard tổng quan</a>
                            <a class="account-dropdown__item" href="@Url.Action("Index", "Products")">Danh sách sản phẩm</a>
                            <a class="account-dropdown__item is-danger" href="@Url.Action("Logout", "Admin")">Đăng xuất</a>
                        </div>
                    </div>
                </div>
            </header>

            <main class="dashboard-scroll">
                <div class="tables-page">
                    <section class="table-board" aria-label="Danh sách bàn">
                        <div class="table-board__grid">
                            @foreach (var item in tables)
                            {
                                var hasOrderInfo = !string.IsNullOrWhiteSpace(item.TimeLabel) || !string.IsNullOrWhiteSpace(item.AmountLabel);

                                <button type="button"
                                        class="restaurant-table-card @item.CardClass"
                                        data-table-card
                                        data-table-id="@item.TableId"
                                        aria-label="Xem thông tin bàn @item.TableId">
                                    <span class="restaurant-table-card__title">Bàn @item.TableId</span>

                                    <span class="restaurant-table-card__status status-pill status-pill--@item.StatusClass">
                                        @item.StatusLabel
                                    </span>

                                    <span class="restaurant-table-card__seats">
                                        <svg viewBox="0 0 24 24" aria-hidden="true">
                                            <path d="M16 11a4 4 0 1 0-3.46-6"></path>
                                            <path d="M12.5 20a7 7 0 0 0-13 0"></path>
                                            <path d="M7 11a4 4 0 1 0 0-8 4 4 0 0 0 0 8Z"></path>
                                            <path d="M19 20a5.5 5.5 0 0 0-4-5.28"></path>
                                        </svg>
                                        <span>@(item.Seats > 0 ? $"{item.Seats} chỗ" : "Chưa có số chỗ")</span>
                                    </span>

                                    @if (hasOrderInfo)
                                    {
                                        <span class="restaurant-table-card__divider"></span>

                                        <span class="restaurant-table-card__meta">
                                            @if (!string.IsNullOrWhiteSpace(item.TimeLabel))
                                            {
                                                <span class="restaurant-table-card__meta-line @(item.StatusClass == "reserved" ? "is-blue" : string.Empty)">
                                                    <svg viewBox="0 0 24 24" aria-hidden="true">
                                                        <circle cx="12" cy="12" r="9"></circle>
                                                        <path d="M12 7v5l3 2"></path>
                                                    </svg>
                                                    <span>@item.TimeLabel</span>
                                                </span>
                                            }

                                            @if (!string.IsNullOrWhiteSpace(item.AmountLabel))
                                            {
                                                <span class="restaurant-table-card__meta-line is-money">
                                                    <span class="restaurant-table-card__money-symbol">$</span>
                                                    <span>@item.AmountLabel</span>
                                                </span>
                                            }
                                        </span>
                                    }
                                </button>
                            }
                        </div>
                    </section>
                </div>
            </main>
        </div>
    </div>

    <div id="component-FormBuy"></div>

    <div class="table-loading-overlay" id="tableLoadingOverlay" hidden>
        <div class="table-loading-card">
            <span class="table-loading-spinner"></span>
            <span>Đang tải thông tin bàn...</span>
        </div>
    </div>

    <audio id="success-sound" src="/asset/sound/Thongbao1.mp3" preload="auto"></audio>

    <script src="~/assets/plugins/Jquery/dist/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/5.0.9/signalr.min.js"></script>
    <script src="~/assets/js/admin-tables.js" asp-append-version="true"></script>
</body>
</html>
